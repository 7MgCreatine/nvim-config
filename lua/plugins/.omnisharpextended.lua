return
{
    "Hoffs/omnisharp-extended-lsp.nvim",
    dependencies = { "nvim-lua/plenary.nvim" }, -- falls noch nicht installiert
    config = function()
        -- Keine spezielle Konfiguration erforderlich, da die Befehle und Handler direkt verwendet werden.
    end
}
